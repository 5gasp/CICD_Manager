version: "3"
services:
    db:
        image: postgres
        ports:
            - "5432:5432"
        volumes:
            - ./data/db:/var/lib/postgresql/data
        environment:
            - POSTGRES_DB=5gasp
            - POSTGRES_USER=postgres
            - POSTGRES_PASSWORD=postgres